function Titulo ({tituloApp, subTituloApp}) {
        return(
            <>
            
            <h1 className="text-center text-lg title">{tituloApp}</h1>
            <h2 className="text-center subTitle">{subTituloApp}</h2>

            </>
        )
}

export default Titulo