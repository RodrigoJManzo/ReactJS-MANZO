function Titulo ({tituloApp, subTituloApp}) {
        return(
            <>
            
            <h1 className="title">{tituloApp}</h1>
            <h2 className="subTitle">{subTituloApp}</h2>

            </>
        )
}

export default Titulo